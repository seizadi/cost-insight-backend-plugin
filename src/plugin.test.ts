import { costInsightsBackendPlugin } from './plugin';

describe('cost-insights-backend-plugin', () => {
    it('should export plugin', () => {
        expect(costInsightsBackendPlugin).toBeDefined();
    });
});
